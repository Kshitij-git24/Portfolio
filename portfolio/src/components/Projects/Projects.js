import React from "react";
import "./projects.scss";

const Projects = ({ projects }) => {
  return <div className="card-container">{projects.map((project) => {})}</div>;
};

export default Projects;
